import 'package:flutter/material.dart';

class SquaresButtonsAtSecondPage extends StatelessWidget {
  const SquaresButtonsAtSecondPage({Key? key,});


  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
          children: [

          ],
        ),
      ],
    );
  }
}
